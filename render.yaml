services:
  - type: web
    name: evolution-api
    env: node
    buildCommand: "npm install"
    startCommand: "npm run start"
    envVars:
      - key: SERVER_PORT
        value: 8080
      - key: AUTHENTICATION_TYPE
        value: apikey
