// --- SEU package.json ATUAL (EXEMPLO) ---

{
  "name": "evolution-api",
  "version": "2.3.6",
  "description": "",
  "main": "./src/main.ts",
  "scripts": {
    "start": "tsx ./src/main.ts"
    // O script "build" pode estar faltando, ou não tem o "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.x.x"
  },
  "devDependencies": {
    "prisma": "^5.x.x",
    "tsx": "^4.x.x"
  }
}
